import { Container } from 'react-bootstrap';

const ListKeyBoard = () => {
  return (
    <div className="col-10">
      <Container>Danh sách bàn phím</Container>
    </div>
  );
};

export default ListKeyBoard;
